
### 自回归模型“厚涂与负形”奖励机制：像素级债务与延迟清算

传统的单步重建误差奖励会导致模型陷入局部最优：不敢使用“先铺大色块，再用底色修剪（负形）”的高效策略，因为铺大色块必定造成边缘溢出，带来巨大的单步惩罚。

为解决此问题，我们引入**“像素级债务与延迟清算”**机制。核心逻辑是将单步误差拆分为“向好”的正奖励和“向坏”的债务，并允许模型在后续步骤中“还债”。

#### 1. 变量定义
*   $E_t(i)$：第 $t$ 步后像素 $i$ 的重建误差（如 L1 或 MSE）。
*   $\Delta E_t(i)$：单步误差变化量，$\Delta E_t(i) = E_t(i) - E_{t-1}(i)$。
*   $D_t(i)$：第 $t$ 步后像素 $i$ 的累计债务。

#### 2. 奖励与记账规则
*   **即时奖励（鼓励铺色）**：当笔画覆盖像素 $i$ 且误差降低时（$\Delta E_t(i) < 0$），给予即时正向奖励 $R_{t}^{+}(i) = |\Delta E_t(i)|$。
*   **债务记账（暂不惩罚）**：当笔画导致误差升高时（$\Delta E_t(i) > 0$），**不给予即时惩罚**，而是将新增误差计入该像素的账单：$D_t(i) = D_{t-1}(i) + \Delta E_t(i)$。这在机制上保护了前期的大面积铺色探索。
*   **债务偿还（赋予负形意义）**：后续步骤中使用负形或覆盖修复了像素 $i$（$\Delta E_{t+k}(i) < 0$）时，除了获得即时奖励，同时核减账单债务：$D_{t+k}(i) = \max(0, D_{t+k-1}(i) - |\Delta E_{t+k}(i)|)$。
*   **终局清算（防止赖账）**：在序列最后一步（总步数为 $T$），所有未被偿还的债务必须作为巨额惩罚一次性扣除：$R_{penalty} = -\lambda \sum_i D_T(i)$ （其中 $\lambda \ge 1$）。

#### 3. 策略收敛推演
在引入固定的单步步数惩罚（Step Penalty = $C$），且最终画面与目标一致的前提下对比：
*   **细笔拼凑策略（如4笔画边框）**：步数多，不产生债务，获得净修复奖励，总成本扣除 $4C$。
*   **厚涂负形策略（1笔大块 + 1笔负形切割）**：步数少。第1步借债，第2步还债。终局债务为0。获得同样的净修复奖励，总成本仅扣除 $2C$。

由于像素级积分收益在数学上被完全等量配平，自回归模型在策略梯度的驱动下，会自动避开高步数惩罚，稳定收敛到效率更高的“厚涂/负形”画法。
